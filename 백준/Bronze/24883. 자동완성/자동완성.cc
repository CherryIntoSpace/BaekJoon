#include <iostream>
#include <string>
using namespace std;
int main(int argc, char** argv) {ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
	
	char c;
	
	cin>>c;
	
	if((c == 'N') || (c == 'n')){
		cout<<"Naver D2";
	}
	else{
		cout<<"Naver Whale";
	}
}